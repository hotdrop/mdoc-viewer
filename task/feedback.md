# 2026/5/16 11:24 フィードバック

## 作業内容
- トップページの固定表示ドキュメントを `schedules/schedules.txt` と `schedules/release_note.txt` から、ドキュメントルート直下の `index.txt` に一本化した。
- ローカル確認用に `test_docs/index.txt` を追加し、関連する仕様書とテストを更新した。

## 開発改善フィードバック
- 既存ルール・手順が障壁になった点: トップページ固定ドキュメントの実体パスが仕様書と実装定数に分散しており、ローカルとCloud Storageでどこに置くべきかを把握するのに複数ファイルの確認が必要だった。
- 改善した方がよいルール・手順: トップページなど特殊用途ドキュメントは、通常のドキュメントツリーとは別に「固定用途ドキュメント一覧」として仕様書へ明記すると確認しやすい。
- 追加した方がよいルール・手順: Cloud Storageへ配置する最小オブジェクト構成例に `index.txt` を含め、ローカル `test_docs/` との対応関係をREADMEに短く記載するとよい。
- docs/README/タスクメモ/テストなどへ反映した方がよい点: `/`、`/viewer`、`/viewer/index` がいずれも `index.txt` に関係することを手動確認手順に追加するとよい。

## 分類
- タスク固有: 今回の `index.txt` 一本化に伴うトップページ固定ドキュメントの配置説明と手動確認手順。
- 恒久対応候補: 特殊用途ドキュメントを仕様書またはREADMEに一覧化し、通常の階層表示対象と区別する運用。

## 更新先候補
- AGENTS.md: 特殊用途ドキュメントの一覧化を恒久ルールにする場合のみ候補。
- .codex/skills/md-doc-viewer/SKILL.md: トップページや固定ドキュメントを変更する際の確認観点を追加する候補。
- docs/README/task/tests など: `docs/specification.md`、README、手動確認手順、必要に応じてトップページ固定ドキュメントのテスト。

# 2026/5/16 11:51 フィードバック

## 作業内容
- ディレクトリ配下の `index.txt` をフォルダ入口文書として扱い、`mobileapp/index.txt` などの公開リンクを `/viewer/mobileapp` に正規化した。
- ドキュメントツリーで `index` 子項目を重複表示せず、チームフォルダ自体をクリック可能にした。
- 仕様書とパス正規化・ツリー生成・前後ナビのテストを更新した。

## 開発改善フィードバック
- 既存ルール・手順が障壁になった点: `index.txt` の扱いがトップページ用途とディレクトリ入口用途の両方にまたがるため、URL正規化・ツリー表示・検索リンクの期待値を横断確認する必要があった。
- 改善した方がよいルール・手順: ディレクトリ入口文書のURL規則と、`/viewer/foo` と `/viewer/foo/index` の正規化方針を仕様書のルーティング章に表形式で固定するとよい。
- 追加した方がよいルール・手順: ツリー生成を変更するタスクでは、代表 `index.txt`、通常文書、ルート `index.txt` の3ケースを最小テスト観点として明記するとよい。
- docs/README/タスクメモ/テストなどへ反映した方がよい点: READMEまたは手動確認手順に、`mobileapp/index.txt`、`server/index.txt`、`supportqa/index.txt` を含む推奨ディレクトリ例を追加するとよい。

## 分類
- タスク固有: チーム別ドキュメント階層とフォルダ入口文書のURL・ナビ表示確認。
- 恒久対応候補: ディレクトリ入口文書の正規化規則を、今後のツリー・検索・最近更新一覧変更時の共通確認観点にする。

## 更新先候補
- AGENTS.md: フォルダ入口文書の確認観点を恒久ルール化する場合のみ候補。
- .codex/skills/md-doc-viewer/SKILL.md: ツリー表示やリンク正規化を触る際の代表 `index.txt` テスト観点を追加する候補。
- docs/README/task/tests など: `docs/specification.md`、READMEの構成例、手動確認手順、ツリー生成テスト。

# 2026/5/16 11:57 フィードバック

## 作業内容
- ローカル確認用の `test_docs` に `mobileapp`、`server`、`supportqa` のチーム別入口文書と子文書を追加した。
- トップドキュメントから各チーム入口へ移動できるリンクを追加した。

## 開発改善フィードバック
- 既存ルール・手順が障壁になった点: ローカル確認用データの推奨構成がREADMEや手動確認手順にまとまっていないため、どの文書を開けば階層構造の期待挙動を確認できるかが分かりにくかった。
- 改善した方がよいルール・手順: `test_docs` のサンプル構成と確認すべきURLをREADMEまたはタスクメモに一覧化するとよい。
- 追加した方がよいルール・手順: ローカル確認データを追加する際は、トップから辿れるリンク、チーム入口、子文書、相対リンクの4点を最低限含めるとよい。
- docs/README/タスクメモ/テストなどへ反映した方がよい点: `/`, `/viewer/mobileapp`, `/viewer/server`, `/viewer/supportqa`, `/search?q=Mobile` のような手動確認URL例を追加するとよい。

## 分類
- タスク固有: チーム別階層構造をローカルで確認するための `test_docs` サンプル追加。
- 恒久対応候補: ローカル確認用ドキュメントの標準サンプル構成と手動確認URLの整備。

## 更新先候補
- AGENTS.md: ローカル確認データの標準構成を恒久ルール化する場合のみ候補。
- .codex/skills/md-doc-viewer/SKILL.md: UI確認が必要なタスクで `test_docs` の代表URLを確認する手順を追加する候補。
- docs/README/task/tests など: README、手動確認手順、`task/` の確認メモ。
